<?php
if (isset($_GET['email']) && isset($_GET['token'])) {
    $email = $_GET['email'];
    $token = $_GET['token'];

    // التحقق من صحة الرابط (في حالة استخدام قاعدة بيانات، يجب التحقق من الرمز المخزن)
    echo "تم التحقق من البريد الإلكتروني بنجاح! مرحبًا بك.";
} else {
    echo "الرابط غير صالح.";
}
?>
